{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hypereikon.online/schemas/inside-valdivia-cauce-operation-history-lock-1.json",
  "type": "object",
  "required": ["schema", "source", "contracts"],
  "properties": {
    "schema": { "const": "inside-valdivia.cauce-operation-history-lock/1" },
    "source": {
      "type": "object",
      "required": ["repository", "commit", "catalog_hash"],
      "properties": {
        "repository": { "type": "string", "minLength": 1 },
        "commit": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
        "catalog_hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      },
      "additionalProperties": false
    },
    "contracts": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "version", "contract_hash", "source_commit"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)+$" },
          "version": { "type": "integer", "minimum": 1 },
          "contract_hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
          "source_commit": { "type": "string", "pattern": "^[0-9a-f]{40}$" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
