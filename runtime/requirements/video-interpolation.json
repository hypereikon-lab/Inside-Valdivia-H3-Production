{
  "schema": "comfy.runtime-requirements/1",
  "id": "inside-valdivia-video-interpolation",
  "required_endpoints": ["features", "system_stats", "object_info", "queue"],
  "required_node_types": [
    "CaucePlanFrameInterpolation",
    "CreateVideo",
    "FrameInterpolate",
    "FrameInterpolationModelLoader",
    "GetVideoComponents",
    "LoadVideo",
    "SaveVideo"
  ],
  "node_type_groups": [],
  "required_models": ["film_net_fp16.safetensors", "rife_v4.26.safetensors"],
  "hardware": {
    "minimum_total_ram_bytes": 60000000000,
    "minimum_total_vram_bytes": 30000000000,
    "device_name_contains": "5090"
  },
  "require_queue_idle": true,
  "manual_checks": [
    "Confirm ComfyUI is 0.34.0 or newer and exposes the native FrameInterpolationModelLoader and FrameInterpolate contracts.",
    "Confirm both model files match the declared Comfy-Org revisions and SHA-256 values; no interpolation custom node should be installed.",
    "Confirm the decoded source frame count and fps before binding the CAUCE plan.",
    "Confirm at least 30 GB of free local storage before downloading weights or saving a long high-fps output.",
    "Confirm exact endpoint and source-anchor preservation before assigning a visual verdict."
  ]
}
